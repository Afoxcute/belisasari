# Solana RPC (required for balance, swap, tokens - public RPC often returns 403 from server)
# Get a free key: https://helius.dev or https://quicknode.com or https://alchemy.com
RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY

# Optional: if you need RPC in the browser (e.g. wallet sends tx), use same URL
# NEXT_PUBLIC_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY

# Zerion API (optional): for Portfolio page - wallet performance, token balances, DeFi positions, tx history
# Get a key: https://dashboard.zerion.io
# ZERION_API_KEY=your_zerion_api_key
